# directories to exclude from the slocate database:
PRUNEPATHS="/tmp /var/tmp /usr/tmp /afs /net /proc /var/spool /var/cache"

# filesystems to exclude from the slocate database:
PRUNEFS="nfs smbfs ncpfs"
